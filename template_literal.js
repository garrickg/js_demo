const person = {
  name: 'Garrick Gunn',
  location: 'Victoria'
}

console.log('Hello ' + person.name + ' from ' + person.location + '.')

// using template literals
// console.log(`Hello ${person.name} from ${person.location}.`)
